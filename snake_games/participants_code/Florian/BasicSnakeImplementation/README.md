Startup
-----------------
Run from base folder: `python3 -m BasicSnakeImplementation.main`



Background music
-----------------
Place a small OGG file at `BasicSnakeImplementation/assets/bgm.ogg`.
If the file is missing or audio init fails, the game runs silently.
